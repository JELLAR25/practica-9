#include <stdio.h>

void main()
{
  int list{5};
  int i, calf=0;
  float prom;
  
  for (i = 0; i<5; i++)
  {
    printf("introducir la calificacion numero %d: ", i + l );
    scanf("%d", &list[i]);
  }

  for (i=0; i<5; i++)
  {
    calif = calif + list[i];
  }

  prom = calif / 5;
  prinf("el promedio de las calificaciones es: %f\n", prom); 
